
const secure = true
const httpOnly = true
const sameSite = 'none'
const maxAge = 3600 * 1000


const options = {
    secure:secure,
    httpOnly:httpOnly,
    sameSite:sameSite,
    maxAge:maxAge
}


module.exports = options
